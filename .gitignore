# Personal configuration (contains SMTP creds, admin password)
config.yaml

# Database
*.db
data/

# Photos (personal assets)
photos/

# Uploaded resume PDF
uploads/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
venv/
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Phase 31 — Playwright test artefacts (videos, screenshots, traces).
# pytest-playwright writes these under the project root when a test
# fails; CI uploads them as a job artifact, locally they're disposable.
test-results/
playwright-report/
.hypothesis/

# Local tooling state
.claude/

# Environment
.env
.flaskenv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Container build artifacts
*.tar

# Mutation testing (Phase 33) — mutmut writes its mutated source tree
# plus per-file .meta state and stats JSONs into mutants/.
mutants/
